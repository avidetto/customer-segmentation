name: customer-segmentation
description: Customer segmentation app using Delta clustering and React UI
python:
  requirements_file: app/requirements.txt
build:
  commands:
    - command: ["bash", "-c", "cd web && npm install && npm run build 2>&1 || echo 'Build failed or npm not available'"]
command:
  - python
  - "-m"
  - "uvicorn"
  - "app.api:app"
  - "--host"
  - "0.0.0.0"
  - "--port"
  - "$DATABRICKS_APP_PORT"
